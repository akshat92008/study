// utils/ocr.ts - placeholder OCR utility
export async function runOCR(imageBase64: string, mimeType: string): Promise<string> {
  // Placeholder: no OCR performed, returns empty string.
  return '';
}
