// lib/alerts/slackAlert.ts

/**
 * Send an alert message to Slack (stub).
 */
export async function sendSlackAlert(title: string, message: string): Promise<void> {
  console.log(`[SLACK ALERT] ${title}: ${message}`);
}
