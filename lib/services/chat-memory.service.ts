// lib/services/chat-memory.service.ts
// ⚠️  TOMBSTONE — DO NOT USE ⚠️
// Use lib/services/chatMemoryService.ts (note: no hyphen, camelCase).
// This file previously contained an older implementation. Both implement ChatMemoryService
// but this one is the wrong one. The confusion between these two file names is a known bug.

export class ChatMemoryService {
  constructor() {
    throw new Error(
      'Wrong ChatMemoryService imported. Use: import { ChatMemoryService } from "@/lib/services/chatMemoryService"'
    );
  }
}
